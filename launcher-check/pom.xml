<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.progmatic</groupId>
        <artifactId>JLeTrain</artifactId>
        <version>1.0.0-beta.5-SNAPSHOT</version>
    </parent>
    <artifactId>JLeTrain-launcher-check</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>com.progmatic</groupId>
            <artifactId>JLeTrain-core</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.4.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>jar-with-dependencies</shadedClassifierName>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>letrain.LeTrainCheck</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>jlink-jre</id>
                        <phase>package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>jlink</executable>
                            <arguments>
                                <argument>--add-modules</argument>
                                <argument>java.base,java.logging,jdk.unsupported</argument>
                                <argument>--output</argument>
                                <argument>${maven.multiModuleProjectDirectory}/output/letrain-check/lib/runtime</argument>
                                <argument>--strip-debug</argument>
                                <argument>--no-man-pages</argument>
                                <argument>--no-header-files</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>clean-output</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <delete dir="${maven.multiModuleProjectDirectory}/output/letrain-check"/>
                            </target>
                        </configuration>
                    </execution>
                    <execution>
                        <id>create-launchers</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <mkdir dir="${maven.multiModuleProjectDirectory}/output/letrain-check/lib/app"/>
                                <copy file="${project.build.directory}/${project.build.finalName}-jar-with-dependencies.jar"
                                      tofile="${maven.multiModuleProjectDirectory}/output/letrain-check/lib/app/${project.build.finalName}-jar-with-dependencies.jar"/>

                                <mkdir dir="${maven.multiModuleProjectDirectory}/output/letrain-check/bin"/>
                                <echo file="${maven.multiModuleProjectDirectory}/output/letrain-check/bin/letrain-check">#!/bin/sh
DIR="$(cd "$(dirname "$0")" &amp;&amp; pwd)"
"$DIR/../lib/runtime/bin/java" -jar "$DIR/../lib/app/${project.build.finalName}-jar-with-dependencies.jar" "$@"
</echo>
                                <chmod file="${maven.multiModuleProjectDirectory}/output/letrain-check/bin/letrain-check" perm="755"/>

                                <echo file="${maven.multiModuleProjectDirectory}/output/letrain-check/bin/letrain-check.bat">@echo off
set DIR=%~dp0
"%DIR%\..\lib\runtime\bin\java.exe" -jar "%DIR%\..\lib\app\${project.build.finalName}-jar-with-dependencies.jar" %*
</echo>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
