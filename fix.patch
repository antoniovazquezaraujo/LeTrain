--- src/main/java/letrain/vehicle/rail/impl/TrainMovementManager.java
+++ src/main/java/letrain/vehicle/rail/impl/TrainMovementManager.java
@@ -432,9 +432,10 @@
 
         for (int i = 0; i < order.size(); i++) {
             Linker linker = order.get(i);
-            if (linker instanceof Locomotive) {
-                continue;
-            }
+            // Only skip the director linker; other locomotives should follow the train's physical direction
+            //if (linker instanceof Locomotive) {
+            //    continue;
+            //}
+            if (linker == tractor) continue;
 
             Track track = linker.getTrack();
             if (track == null) continue;
