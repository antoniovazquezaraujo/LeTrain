package letrain.mvp.impl.delegates;

import letrain.mvp.GameModel;
import letrain.mvp.GameView;

public class FactoryPlatformCreationDelegate extends GamePresenterDelegate {
    public FactoryPlatformCreationDelegate(GameModel model, GameView view) {
        super(model, view);
    }
}
