package letrain.mvp.impl.delegates;

import letrain.mvp.GameModel;
import letrain.mvp.GameView;

public class FactoryPlatformControlDelegate extends GamePresenterDelegate {
    public FactoryPlatformControlDelegate(GameModel model, GameView view) {
        super(model, view);
    }
}
