package letrain.mvp.impl.delegates;

import letrain.mvp.GameModel;
import letrain.mvp.GameView;

public class LoadPlatformControlDelegate extends GamePresenterDelegate {
    public LoadPlatformControlDelegate(GameModel model, GameView view) {
        super(model, view);
    }
}
