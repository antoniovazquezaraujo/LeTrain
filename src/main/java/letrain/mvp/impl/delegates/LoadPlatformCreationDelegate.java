package letrain.mvp.impl.delegates;

import letrain.mvp.GameModel;
import letrain.mvp.GameView;

public class LoadPlatformCreationDelegate extends GamePresenterDelegate {
    public LoadPlatformCreationDelegate(GameModel model, GameView view) {
        super(model, view);
    }
}
