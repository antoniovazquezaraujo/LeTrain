package letrain.mvp.impl.delegates;

import letrain.mvp.GameModel;
import letrain.mvp.GameView;

public class TrackDeletionDelegate extends GamePresenterDelegate {
    public TrackDeletionDelegate(GameModel model, GameView view) {
        super(model, view);
    }
}
