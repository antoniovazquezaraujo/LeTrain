<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.progmatic</groupId>
        <artifactId>JLeTrain</artifactId>
        <version>1.0.0-beta.3</version>
    </parent>
    <artifactId>JLeTrain-launcher-graphic</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>com.progmatic</groupId>
            <artifactId>JLeTrain-ui-graphic</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.4.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>jar-with-dependencies</shadedClassifierName>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>letrain.LeTrainGraphic</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>jlink-jre</id>
                        <phase>package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>jlink</executable>
                            <arguments>
                                <argument>--add-modules</argument>
                                <argument>java.base,java.desktop,java.naming,java.prefs,java.sql,jdk.unsupported</argument>
                                <argument>--output</argument>
                                <argument>${maven.multiModuleProjectDirectory}/output/LeTrain/lib/runtime</argument>
                                <argument>--strip-debug</argument>
                                <argument>--no-man-pages</argument>
                                <argument>--no-header-files</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>clean-output</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <delete dir="${maven.multiModuleProjectDirectory}/output/LeTrain"/>
                            </target>
                        </configuration>
                    </execution>
                    <execution>
                        <id>create-sh-launcher</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <!-- Copy the jar into lib/app -->
                                <mkdir dir="${maven.multiModuleProjectDirectory}/output/LeTrain/lib/app"/>
                                <copy file="${project.build.directory}/${project.build.finalName}-jar-with-dependencies.jar"
                                      tofile="${maven.multiModuleProjectDirectory}/output/LeTrain/lib/app/${project.build.finalName}-jar-with-dependencies.jar"/>

                                <!-- Create the shell script -->
                                <mkdir dir="${maven.multiModuleProjectDirectory}/output/LeTrain/bin"/>
                                <echo file="${maven.multiModuleProjectDirectory}/output/LeTrain/bin/LeTrain.sh">#!/bin/sh
DIR="$(cd "$(dirname "$0")" &amp;&amp; pwd)"
"$DIR/../lib/runtime/bin/java" -jar "$DIR/../lib/app/${project.build.finalName}-jar-with-dependencies.jar"
</echo>
                                <!-- Make it executable -->
                                <chmod file="${maven.multiModuleProjectDirectory}/output/LeTrain/bin/LeTrain.sh" perm="755"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
